{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "cartas/erros-evolucao-mensal",
  "description": "Série histórica mensal de erros abertos vs. resolvidos nas cartas de serviço (domínio Qualidade, ADR-005), ordenado cronologicamente.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["mes", "abertos", "resolvidos"],
    "properties": {
      "mes": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}$" },
      "abertos": { "type": "integer", "minimum": 0 },
      "resolvidos": { "type": "integer", "minimum": 0 }
    }
  }
}
