{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ga4/funil",
  "description": "1 chave por período fixo (dia/semana/mes/ano) — ver ADR-007 (v2)",
  "type": "object",
  "required": ["dia", "semana", "mes", "ano"],
  "properties": {
    "dia": { "$ref": "#/definitions/linhas" },
    "semana": { "$ref": "#/definitions/linhas" },
    "mes": { "$ref": "#/definitions/linhas" },
    "ano": { "$ref": "#/definitions/linhas" }
  },
  "definitions": {
    "linhas": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["evento", "usuarios"],
        "properties": {
          "evento": { "type": "string", "enum": ["first_open", "session_start", "screen_view", "user_engagement"] },
          "usuarios": { "type": "integer", "minimum": 0 }
        }
      }
    }
  }
}
